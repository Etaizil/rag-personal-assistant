# Canonicar Overview (sample)

Canonicar's goal is to train a RL agent to drive autonomously. Experiment is being held in the CARLA simulator. It uses ROS2 nodes, a multi‑sensor stack (semantic segmentation camera, LiDAR, IMU), and a PPO RL pipeline to control steering/throttle.